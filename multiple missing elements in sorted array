#include <iostream>
#include <climits>
#include <vector>
using namespace std;


struct Array{
    int A[20];
    int size;
    int length;
};

void multipleMissing(struct Array *arr){
    int i;
    int diff = arr->A[i] - 0;
    for(i=0;i<arr->length;i++){
        if(arr->A[i]-i != diff ){
            while(diff<arr->A[i]-i){
                cout<<diff+i<<endl;
                diff++;
            }
        }
    }
}
